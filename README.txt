JEE Chapter Tracker PWA

Upload this folder to an HTTPS host (for example GitHub Pages).
Open the HTTPS site in Chrome on Android -> ⋮ -> Install app/Add to Home screen.

Progress is stored in browser localStorage. Use Export Backup before clearing
browser data or changing devices, then Import Backup on the new device.
